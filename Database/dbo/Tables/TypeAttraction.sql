CREATE TABLE [dbo].[TypeAttraction] (
    [TypeAttractionId]   TINYINT       NOT NULL,
    [TypaAttractionName] NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_TypeAttraction] PRIMARY KEY CLUSTERED ([TypeAttractionId] ASC)
);

