CREATE TABLE [dbo].[TypePublicity] (
    [TypePublicityId]   TINYINT       NOT NULL,
    [TypePublicityName] NVARCHAR (10) NOT NULL,
    CONSTRAINT [PK_TypePublicity] PRIMARY KEY CLUSTERED ([TypePublicityId] ASC)
);

